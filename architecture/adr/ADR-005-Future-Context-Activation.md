# ADR-005 Future Context Activation

Status: Accepted

## Decision
Future bounded contexts are activated only when justified by business capability.

No speculative implementation.
Each new context requires:
- Business capability
- Bounded context
- Domain model
- ADR (if architectural impact exists)
